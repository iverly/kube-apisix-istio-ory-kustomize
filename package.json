{
  "name": "kube-apisix-linkerd-ory-kustomize",
  "version": "1.0.0",
  "description": "This is an example to deploy apisix as gateway with linkerd and the ory stack using kustomize",
  "scripts": {
    "kube:cluster:create": "k3d cluster create kube-apisix-linkerd-ory-kustomize -c ./config/k3d/config.yaml",
    "kube:cluster:delete": "k3d cluster delete kube-apisix-linkerd-ory-kustomize",
    "kube:linkerd:pre-install:check": "linkerd check --pre",
    "kube:linkerd:post-install:check": "linkerd check --proxy --namespace apisix",
    "kube:linkerd:apply": "kubectl apply -k ./kustomize/linkerd",
    "kube:linkerd:delete": "kubectl delete -k ./kustomize/linkerd",
    "kube:apisix:apply": "kubectl apply -k ./kustomize/apisix",
    "kube:apisix:delete": "kubectl delete -k ./kustomize/apisix"
  },
  "author": "iverly <github@iverly.net>",
  "license": "Apache-2.0"
}
